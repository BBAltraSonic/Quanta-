import 'package:flutter/material.dart';

// Colors
const kPrimaryColor = const Color(0xFFFF2E2E); // Primary red tuned to plan
const kBackgroundColor = const Color(
  0xFF0F0F0F,
); // Dark background per plan 0E-11
const kCardColor = const Color(0xFF2C2C2C); // Darker gray for cards/elements
const kTextColor = Colors.white; // Colors.white is already a const
const kLightTextColor = const Color(
  0xFFB0B0B0,
); // Lighter gray for secondary text

// Padding and Margins
const kDefaultPadding = 16.0;
const kDefaultMargin = 16.0;

// Border Radius
const kDefaultBorderRadius = 12.0;

// Text Styles (Example - will be refined in main.dart)
const kHeadingTextStyle = const TextStyle(
  color: kTextColor,
  fontSize: 24,
  fontWeight: FontWeight.bold,
);

const kBodyTextStyle = const TextStyle(color: kTextColor, fontSize: 16);

const kCaptionTextStyle = const TextStyle(color: kLightTextColor, fontSize: 12);
